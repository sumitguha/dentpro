%ul
  %li= "#{link_to("Home", root_url)}"
  %li= "#{link_to("What We Do", what_we_do_url)}"
  %li= "#{link_to("Get a Quote!", quote_url)}"
  %li= "#{link_to("Where We Are", locations_url)}"
  %li= "#{link_to("Who We are", who_we_are_url)}"
  %li= "#{link_to("Contact Us", contact_us_url)}"
  %li= "#{link_to("Survey", 'http://spreadsheets.google.com/viewform?formkey=dFM2c0pDazZNZjNwcGoyUnNXa19VZFE6MA', :target => '_blank')}"

